[pytest]
# Pytest configuration for stock-signals
markers =
    live_trading: marks tests as requiring live trading infrastructure (database, paper trading)
    backtest: marks tests as relevant for backtesting validation
    slow: marks tests as slow running (>5s)

# Skip live trading tests by default (they have database setup issues)
addopts = -v -m "not live_trading"
